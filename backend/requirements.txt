fastapi
psycopg[binary]
sqlalchemy
alembic
uvicorn[standard]
bcrypt
PyJWT
# Pinned exactly for accuracy v1: the frozen goldens and every stored session
# accuracy were computed under this python-chess version. See app/accuracy.py
# (CHESS_VERSION_PIN) and docs/session-accuracy-versioning.md before bumping.
chess==1.11.2
# Server-side product/latency analytics (instance API: Posthog(token, host=...);
# capture(distinct_id=, event=, properties=); shutdown() flushes the sender thread)
posthog>=3.25,<4
# Server-side Stockfish for VMU centipawn-loss evaluation
stockfish>=3.28.0
# Maia-2 engine dependencies (requires Python 3.12 or earlier)
# PyTorch does not yet have wheels for Python 3.13+
maia2==0.9
torch>=2.2.0,<2.3.0
numpy>=1.26.0,<2.0.0
# Undeclared transitive deps of maia2
einops
gdown
pandas
pyzstd
requests
tqdm
